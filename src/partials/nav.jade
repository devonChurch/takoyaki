-
    navItems = [
        { heading: 'About', link: '#about'},
        { heading: 'Contact', link: '#contact'},
        { heading: 'Work', link: '#work'}
    ];

nav.nav
    ul.nav__list
        for item in navItems
            li.nav__item: a.nav__link.type--smallCap(href='#{item.link}') #{item.heading}
